---
title: "Drone"
description: "Integrate Drone CI with Orka to run macOS and iOS build pipelines on genuine Apple hardware. Covers ephemeral VM engines, pipeline setup, and configuration."
zendesk_id: 28398573475867
---

[Drone.io](https://www.drone.io/) is a container-native, continuous delivery platform with an open-source philosophy.

The benefits of Drone with MacStadium aren't exclusive to macOS and iOS development. As a web developer, you want to be sure your code works on all browsers — desktop, laptop, and mobile.

## How it works

Drone relies on ephemeral engines. Every time you run your Drone pipeline, Drone spins up a VM in Orka. After the pipeline is complete, the VM is removed.

## Set up the integration

For configuration details, including pipeline syntax, triggers, and steps, see the [Drone MacStadium pipeline documentation](https://docs.drone.io/pipeline/macstadium/syntax/).
