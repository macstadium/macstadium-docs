---
title: "Understanding your MacStadium IP Plan"
description: "Your IP Plan contains networking and access details for your MacStadium environment, including VPN credentials, IP allocations, and host assignments. View it in the My Cloud page of the MacStadium Portal."
zendesk_id: 28230867289883
---

Your IP Plan is the networking and access information for your MacStadium environment: VPN credentials, network ranges, firewall public IPs, and the IP assignments for your servers.

## Where to find it

The IP Plan is available in the **My Cloud** page of the [MacStadium Portal](/macstadium/macstadium-overview/macstadium-portal). The portal replaces the XLSX spreadsheet that MacStadium previously distributed.

1. Sign in to [portal.macstadium.com](https://portal.macstadium.com/login).
2. Open **My Cloud**.
3. Select the data center tab for the environment you want to view (Atlanta, Las Vegas, or Dublin).

## What's on the page

My Cloud groups your environment by data center. For each data center you'll see:

- **Public Servers** — servers reachable on the public internet, with their public IPs and hostnames.
- **Firewalls** — each dedicated firewall has its own section, containing:
  - **IP Allocation** — the networks behind the firewall, with subnet (CIDR), range, mask, and gateway.
  - **Credentials** — VPN username and password for [connecting to the environment](/orka/networking-with-orka-at-macstadium/vpn-connection). Select **Show** to reveal, then copy.
  - **Host Assignments** — servers assigned to each network, with their private IPs and (where applicable) public NAT IPs.

The firewall's public IP is the address you use as the VPN server address and as the on-prem peer when configuring a site-to-site VPN tunnel — see the setup guides for [AWS](/orka/networking-with-orka-at-macstadium/aws-orka-connections), [GCP](/orka/networking-with-orka-at-macstadium/gcp-orka-connections), or [Azure](/iaas/azure/site-to-site-vpn-configuration-with-azure).

## Common fields referenced in other docs

Other MacStadium docs refer to fields by their network names. Use this mapping when you're looking them up in My Cloud:

| Reference | Where to find it in My Cloud |
|-----------|------------------------------|
| VPN server address | The firewall's public IP |
| VPN credentials | Under **Credentials** for the firewall |
| `Private-1` network | A row in the firewall's **IP Allocation** table. **Name** matches the network name; **Subnet ID** is the CIDR you'll need for VPN tunnels. |
| `Outside` network | The firewall's public-facing network in **IP Allocation** |
| `FW1-Outside` | The firewall's public IP — used as the on-prem peer endpoint for site-to-site VPN tunnels |

> Network names vary by environment. If your environment was provisioned with custom network names, use the **IP Allocation** table for the firewall to identify the equivalent networks.
